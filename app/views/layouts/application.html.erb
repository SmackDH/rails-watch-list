<!DOCTYPE html>
<html>
  <head>
    <title>Watchlist</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css" rel="stylesheet" type="text/css">
  </head>
  <body class="">
    <div class="content">
      <nav class="navbar-lewagon sticky-top">
        <div class="container">
          <div class="menu_title text-white p-2">
            <%= link_to lists_path do %>
              <h2>WATCHLIST</h2>
            <% end %>
          </div>
          <div class="menu_links">
            <% if @list != nil && @list.id? %>
              <%= link_to 'ADD BOOKMARK', new_list_bookmark_path(@list), class: "btn-user btn btn-sm btn-primary btn-login" %>
            <% else %>
              <%= link_to 'ADD LIST', new_list_path, class: "btn-user btn btn-sm btn-primary btn-login"%>
            <% end %>
          </div>
        </div>
      </nav>
      <div class="container-mid">
        <%= yield %>
      </div>
    </div>
    <div class="footer">
      <p>Copyright © <a href="https://github.com/smackdh">Mattias</a> | Le Wagon 2022</p>
    </div>
  </body>
</html>
